// import {FavoriteStateType} from 'src/state/favorites/favoritesReducer';
// import {ThemeReducer, MAX_FONT_SIZE, MIN_FONT_SIZE} from './theme/themeReducer';
const foo = 'bar';
export default foo;
// - Favorites -
//
// export function isFavorited(
//   _id: string | number,
//   state: FavoriteStateType,
// ): boolean {
//   const id = String(_id);
//   return !!state.favorites.get(id);
// }
//
// export function getFavoriteIds(state: FavoriteStateType): string[] {
//   return [...state.favorites.keys()];
// }
//
// // - Theme -
//
// export function isInLightMode(state: ThemeReducer): boolean {
//   return state.currentThemeIndex == 0;
// }
//
// export function canIncreaseFontSize(state: ThemeReducer): boolean {
//   return state.fontSize <= MAX_FONT_SIZE;
// }
//
// export function canDecreaseFontSize(state: ThemeReducer): boolean {
//   return state.fontSize >= MIN_FONT_SIZE;
// }
